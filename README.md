# Ocb Example Mod for Cities Skylines II

Example mod with BepInEx preload patching script.

## How to install

Install like any other BepInEx mod into the
plugins folder (extract into `BepInEx/plugins`).
